FROM datomic-base

ENV CONSOLE_PORT 8080

ENV TRANSACTOR_HOST localhost
ENV TRANSACTOR_PORT 4334
ENV TRANSACTOR_PROTOCOL dev

EXPOSE ${CONSOLE_PORT}

CMD bin/console -p ${CONSOLE_PORT} ${TRANSACTOR_PROTOCOL} datomic:${TRANSACTOR_PROTOCOL}://${TRANSACTOR_HOST}:${TRANSACTOR_PORT}/
